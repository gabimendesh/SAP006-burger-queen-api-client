import React from 'react';
import styles from './style.module.css';

const FormSuccess = () => (
  <div className="form-content-right">
    <h1 className={styles.success}>Cadastro realizado com sucesso!</h1>
  </div>
);

export default FormSuccess;
